# Audit findings — 2026-06-06 cleanup pass

Source: 5-lens adversarial Workflow audit (wf_9410d296-8a5; 21 agents, findings
verified by independent refuters; 13 confirmed / 3 refuted). This file tracks
what was FIXED this pass vs what REMAINS (engine-tail + judgment items).

## Fixed this pass

1. **[CRITICAL] Dead sense-line path** — `SENSE_LINES_DIR` pointed at retired
   `v4/grk`; repointed to live `v1.5/grk`, all 260 chapters resynced
   (commit 9a7b8dc; live-site hash-verified).
2. **[CRITICAL] Validator's five verb checks silently dead** since the
   2026-04-17 parse-display reorder (`'verb' not in prs` gates vs tense-initial
   prs strings). Re-gated on the `tvm` code. All 11 checks now live.
3. **[HIGH] η-augment misses** (θέλω/μέλλω class — irregular η prepended to
   consonant-initial stem). New branch in
   `morpheus._detect_augment_from_stems`; cleared 51 of 269 AUGMENT_MISSING
   flags corpus-wide.
4. Mechanical tail (background agent): dead decomposition code deleted from
   generate_chapter.py; Mark 7 em-dash segment leak; stale glosses_report.txt;
   legend optative/pluperfect glyphs; meta/OG tags; scratch/ untracked;
   CLAUDE.md data-path correction. (See agent receipts in the session.)

## True corpus coverage (all 11 checks live, post-η-fix)

| Check | Coverage | Notes |
|---|---|---|
| HAS_SEGS / RENDER_PATH_OK / VERB_HAS_VOICE / PARTICIPLE_HAS_CASE | 100% | |
| AOR_ACT_FORMATIVE | 99.43% (4154/4178) | |
| AOR_PASS_FORMATIVE | 99.69% (1604/1609) | |
| COMPOUND_PREFIX_EXTRACTED | 99.92% (5123/5127) | 4 flags |
| PARTICIPLE_HAS_MARKER | 99.02% (6575/6640) | 65 flags, perf-act ότ/ῶτ class |
| AUGMENT_EXPECTED | 97.14% (7408/7626) | 218 flags remaining |
| NOMINAL_HAS_SUFFIX | 89.68% (47945/53464) | tally includes legit indeclinables |
| PERF_ACT_FORMATIVE | 77.85% (341/438) | 97 flags — weakest check |

The HANDOFF.md "99.5%+ corpus-wide" claim was measured with only 3 of 11
checks alive. Honest summary: render-integrity checks are at 100%; deep verb
morphology is 97-99% with three named clusters below.

## Remaining engine-tail (needs per-class adjudication, NOT mechanical)

1. **AUGMENT_MISSING 218** — dominated by the εὐ-/ηὐ- vacillation class
   (εὐδόκησεν: SBLGNT often prints the UNaugmented εὐ- form — these may be
   legitimately augmentless readings, i.e. validator false-positives needing a
   variant whitelist, not engine misses). Needs per-lemma triage against the
   stems DB.
2. **PERF_ACT_FORMATIVE 77.85%** — flags include root perfects using a κ-less
   second-perfect stem (ἑστάναι from ἵστημι) where the DB ALSO has a κ-stem;
   the `_stem_has_kappa_perfect` gate is per-lemma but should be per-FORM.
   Likely majority false-positive; needs check-logic refinement.
3. **PARTICIPLE_NO_MARKER 65** — perfect active participle ότ/ῶτ marker class
   (ἐνεστῶτα). Engine doesn't extract the perf-ptc marker; decide whether to
   add extraction or exempt the class in the check.
4. **NOMINAL_HAS_SUFFIX 89.68%** — the INDECL_NAMES whitelist is partial;
   extend from a frequency-ranked list of flagged lemmas, or source
   indeclinability from the lexemes DB.
5. **Pericope-adulterae bracket desync (John 8)** — `⟦…⟧` (U+27E6/27E7)
   editorial brackets aren't in `normalize_for_match`, so bracketed words
   desync sense-line matching in John 8. Pre-existing, surfaced during the
   em-dash fix; same fix shape (add to the normalization strip class) but
   verify the chapter's alignment end-to-end after. A few residual bulk
   warnings of the `SL='ὑμῖν;' MG='ὑμῖν'` shape (trailing punctuation in
   sense-line tokens) are also normalization-class candidates.

## Judgment items for Stan (design decisions, not defects)

1. **Color-only case/gender encoding (a11y)** — adding a non-color channel
   (underline styles / glyph affixes) changes the visual design. Decide
   whether/how.
2. **Gloss validator depth** — current ANTI_PATTERNS whitelist is ~17 strings
   + a 1.9%-coverage testset. Expanding it is open-ended quality work; decide
   how much it matters relative to reader-facing priorities.
3. **Validator as build gate** — wire validate_chapter into bulk_generate /
   sync_senselines so a failing chapter blocks the write (audit found nothing
   currently runs the validators automatically). Mechanical to do; the
   judgment is the failure policy (block vs warn).
